# 双手套穿戴修复记录

日期：2026-09-09。

## 交付结果

- 文件：`达妮娅_鞋子手套穿戴修复.blend`。
- 完整修改前备份：`validation/glove_fix_20260909/before_glove_fit.blend`。
- 活动人物骨架：`达妮娅_arm`。继续使用其原有手腕和手指控制。
- 原手势在测试后恢复，鞋子求值顶点最大位移为 0。

## 最终实现

原手套来自另一套手部比例和骨架。原先整体平移后直接 Surface Deform 绑定，不能解决指长、拇指角度、掌宽的差异。

本次用 23 个手腕及手指骨骼关节对应点，将原手套变形到当前手势附近，作为纹理和装饰的适配来源。最终布料使用素体手部拓扑及其骨骼权重，转移原红色手套的 UV 纹理，并封闭原手部皮肤在指甲处的五个开口。腕部装饰独立保留并重新拟合、绑定。

最终对象：

- `左手套`、`右手套`：Armature → 法线间隙 → 指尖余量 → Solidify → 外侧防穿修正。
- `左手套_腕部装饰`、`右手套_腕部装饰`：Surface Deform → 外侧防穿修正。
- `手套变形参考.L/R`：未被遮罩的手部骨骼变形参考；请保留。
- `达妮娅_mesh` 的 `穿戴手套_隐藏内部手部`：隐藏已经被手套完全包住的皮肤与指甲。

**这里采用可逆的穿戴遮罩，不是把所有底层网格做成零几何交叠。** 身体和指甲的原始网格未删除、未移动；遮罩关闭后，求值身体顶点与修改前比较误差为 0。手套与腕饰的防穿目标使用独立手部参考，避免身体遮罩导致参考手部消失。

外侧防穿 Shrinkwrap 使用 `wrap_mode='OUTSIDE'`，只修正需要移到外侧的点。`OUTSIDE_SURFACE` 会把点约束到目标表面，不能用它替代只向外修正。

## 脱下手套的方法

同时隐藏左右手套和左右腕部装饰，然后关闭 `达妮娅_mesh` 上 `穿戴手套_隐藏内部手部` 修改器的视口和渲染开关，原手和指甲就会完整显示。

没有安装自定义属性的一键穿脱驱动。不要只隐藏手套却保留内部手部遮罩，否则手会看起来缺失。

## 验证

`validation/glove_fix_20260909/final_motion_tests.json` 是最终状态测试，不要把较早的试验报告当成交付结果。

双侧同时测试：

- 原拟合姿态。
- 手腕局部 X ±35°、Z ±25°。
- 四指半握拳：各节 X 增量 25°、35°、20°。
- 四指握拳：各节 X 增量 50°、65°、35°；同时拇指弯曲。
- 单独食指弯曲：各节 X 增量 55°、65°、35°。

这些姿态中，四个手套相关对象均有正常求值结果和实际运动，与遮罩后可见身体的 BVH 交叠面对数均为 0。已检查右手弯曲截图、左手穿戴近景，并恢复原姿势。

测试不等于任意极端手势下都有物理碰撞保证；没有对手套自交、左右手互碰或持物碰撞做完整求解。身体形态键、骨骼权重或拓扑后续若改变，须同步更新手套与参考网格。

## 维护注意

`scripts/glove_*`、`rebuild_glove_shell.py`、`refine_glove_clearance.py` 等包含逐次试验与迁移代码，**不是可重跑的安装程序**。请从交付文件继续操作，不要批量重跑这些脚本。试验中使用过的原对象及网格可从完整备份恢复。

最终文件通过另存生成，输入的鞋子修复文件未被此次保存覆盖。已有鞋子结构不在本次改动范围内。
