# 单张参考图反推建模：方法论调研报告

> 背景：枫树场景迭代 10+ 轮后发现"边做边看"效率极低——先建立可量化的
> 分析管线，把参考图变成带数据的规格书，再动手建模。本报告 = 外部调研 +
> 本项目实测验证 + 失败教训。

---

## 1. 问题定义：一张图里到底有什么可恢复的

单图反推建模的信息分三层，必须先分清，否则会在"图上根本没有的信息"上反复返工：

| 层级 | 内容 | 可否恢复 | 手段 |
|---|---|---|---|
| L1 视图平面几何 | 剪影轮廓、各部位宽高比、分叉点位置、分枝在视平面上的角度 | ✅ 直接可测 | 分割 mask + 逐行/逐列边缘追踪 |
| L2 相机参数 | 焦距、俯仰、相对主体距离比例 | ⚠️ 有条件可估 | 消失点(fSpy/img2cube)、GeoCalib 单图标定、地平线线索 |
| L3 深度/背面 | 枝干的前后穿插、遮挡面、真实绝对尺寸 | ❌ 必须假设 | 植物学先验、对称假设、达芬奇规则 |

**核心教训**：剪影只能给你"投影视平面上的形状"，纵深信息必须靠先验补。
先把 L1 全部测干净，L3 用合理假设填，不要反过来。

---

## 2. 分析管线（7 步，每步产出具体数据）

### Step 0 — 图像体检
- 分辨率、EXIF 焦距（照片有，渲染图/AI 图无）、有无桶形畸变
- 判断光源方向（决定哪些暗部是投影不是形体）——本项目参考图是右后强背光，
  主干正面暗，这既是剪影化的便利（暗色易分割），也是陷阱（暗≠枝干）

### Step 1 — 主体分割（本项目实测有效）
```
mask = (亮度 < 阈值) 且 非(红主导像素)
+ 区域门控（地面线以下、已知杂物区置零）
+ 从主体种子点 BFS 取最大连通域  → 自动丢弃散斑噪声和不连通的背景剪影
```
产出：`ref_branches_silhouette.png`（黑形白底）。本项目阈值：lum<95、
排除 r>1.25g 且 r>1.25b 且 r>60。

> 进阶工具：SAM (Segment Anything) 交互式点选分割、rembg/ISNet 自动主体抠图。
> 纯 numpy 阈值法对"深色主体+亮雾背景"的图已够用。

### Step 2 — 相机与投影估计
- **有平行线的场景**（建筑、地板）：fSpy / img2cube(Blender 插件）画两组平行线
  → 解出焦距、相机朝向。本参考图左侧有寺庙屋顶，理论上可用。
- **无平行线**：GeoCalib(ECCV 2024）从单图直接估焦距+重力方向；或假设
  常见参数（焦距 35-50mm 等效、相机略低于主体中心、水平视角）
- **产出**：相机视锥 → 像素坐标 ↔ 视平面比例换算系数 px/m（在主体距离处）

### Step 3 — 尺度归一（所有测量先无量纲化）
**绝对尺寸不可从单图恢复**，先全部归一：
- 选一个基准量：主干可见高度 Ht（或基部直径）
- 一切测量写成 ×Ht 或 ×基部直径
- 赋绝对值时引入一个现实锚点：日本老枫高 8-12m、景观枫主干径 0.6-1.2m
  → 本项目取 Ht=7.2m，反推绝对尺寸

### Step 4 — 骨架测量（剪影 → 数据表）
对 mask 做逐行扫描得主干**中心线 + 半径包络**；逐列扫描得横枝
**中心线 + 锥度**。本项目实测结果：

主干（Ht=7.2m，基部中心 x=0）:
| z/Ht | dx/Ht | r/Ht | 特征 |
|---|---|---|---|
| 0.00 | 0.00 | 0.115 | 基部+板根 |
| 0.35 | -0.10 | 0.088 | 上行偏左 |
| 0.52 | -0.145 | 0.082 | **肘部**（左缘最大外凸）|
| 0.72 | -0.07 | 0.060 | **收腰**（腰/基径比 0.64）|
| 0.82 | -0.02 | 0.072 | **分叉膨大**（多枝汇聚团）|
| 1.00 | -0.12 | 0.045 | 顶端左折 |

右横主枝（逐列实测）：出叉点 (x≈0.52W, y≈0.29H) → 向右**下垂约 10-15°**
（x=550 yc=196 → x=670 yc=218，图坐标 y 向下为正），直径 55px→21px 渐细。

> 注意：本次实测推翻了原假设"右枝微上挑"——它整体**先下行后末端才上挑**。

### Step 5 — 拓扑与形态学参数（树木专属）
林业/植物建模的定量规律（Honda 1971 起，SpeedTree 同款内核）:

- **分枝角**（子枝与母枝夹角）：决定树冠开散度，逐分叉点测量
- **枝长比**（相邻级枝长比）：决定形态紧凑/疏散
- **达芬奇规则**：母枝截面积 ≈ 子枝截面积之和 → 估算分叉后半径：
  r_child = r_parent × (1/√n)，n 为子枝数（实测枫树指数约 1.2-1.5 而非 2)
- **枝径比 aspect ratio**：子枝径/母枝径 <50% 时连接牢固有"枝领"隆起；
  ≈100% 即"共主干"（本图 coLeader 即此型）
- **叶序/枝序**：枫树对生（opposite）——分枝绕轴成对、相邻层错 90°
- **黄金角 137.5°**：枝条绕干的方位角分布参考

### Step 6 — 深度假设（把 2.5D 补成 3D)
剪影只约束视图平面。纵深靠：
- 枝干前后穿插只做"阅读需要"的量（前/后各 1-2 根体现纵深）
- 分枝方位角按对生/黄金角假设均布
- 相机对齐视图平面建的模型，侧面剪影必然失真——**这是单图方法的固有上限**，
  若需要多角度成立，需要第二张参考或接受"英雄角度"取舍

### Step 7 — 验证闭环（量化，不靠眼睛）
```
建完 → 同相机渲黑剪影 → 与参考 mask 做 XOR/IoU 对比
IoU = 交集/并集；逐段（主干/各主枝）分别算 IoU 定位偏差区
```
本项目已落地：p28_silhouette.py 出同分辨率剪影，可直接与
ref_branches_silhouette.png 像素级对比。

---

## 3. 失败教训（本项目实测踩坑记录）

| 错误 | 后果 | 正确做法 |
|---|---|---|
| 没量剪影就先建模 | 迭代 10+ 轮姿态仍不对 | Step 1-4 必须先出数据表 |
| 把主干当平滑 S | 实际是折线+收腰+膨大 | 逐行描半径包络再说 |
| 枝条求多求密 | 树冠变荆棘丛 | 主枝少而长，密度交给叶子 |
| 拧绳棱脊当"老树感" | 像麻绳/盆景 | 先判断树皮类型：枫=光滑肌肉棱 |
| 枝干纹理一套参数 | 细枝变干裂漆皮 | 材质随枝径分级（Bark/BarkFine)|
| 凭感觉判断"像不像" | 每轮都在打转 | 剪影 XOR/IoU 量化对比 |

## 4. 工具清单

| 用途 | 工具 |
|---|---|
| 分割 | 阈值+BFS（本项目）/ SAM / rembg |
| 相机匹配 | fSpy、img2cube(Blender 插件）、GeoCalib |
| 深度估计 | Depth Anything V2、Marigold（出深度图辅助判断前后关系）|
| 树木生成器参考 | SpeedTree（其生成参数表 = 现成的形态学 schema)|
| 剪影验证 | 黑自发光材质 + 白世界，同分辨率渲染 XOR |

## 5. 规格书模板（建模前必须填满）

```
[主体] 总高 ___m, 最大冠幅 ___m, 高宽比 ___
[主干] 中心线表 (z/Ht, dx/Ht, dy/Ht, r/Ht) ×N点
       横截面形状 ___棱 棱深___, 扭转 ___°/全长, 特征点: 肘部/收腰/膨大位置
[枝序] 每根枝: {父枝, 出枝点z/Ht, 方位角°, 分枝角°, 基径r/Ht, 长/Ht, 锥度曲线}
[材质] 树皮类型(光滑/浅裂/深裂), 色板采样 ×5, 苔藓覆盖规则
[验证] 剪影IoU目标 ≥0.8, 分段IoU: 主干___, 右枝___, 冠部___
```

## 6. 对本项目的直接结论

1. 剪影法已把主干外轮廓测准了（p27 已对上大部分），**剩余差距在
   枝干纵深姿态**——单图已榨干，若要再逼近需要第二角度参考或接受现状
2. 下一步树叶阶段用同款流程：先分割叶团 mask → 测叶团包络（冠幅/厚度/
   边缘破碎度）→ 再定散布参数，不要凭感觉撒
3. 相机参数没估过——建议用 GeoCalib 或 fSpy 对参考图跑一次，
   拿到焦距后重设 Blender 相机，剪影对齐精度会再上一层
