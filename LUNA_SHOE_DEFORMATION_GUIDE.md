# 给 Luna：保持现有姿势的鞋子变形修复指导

记录日期：2026-09-09。本文记录本次实际实施的方法、验证结果和后续操作边界。

## 1. 用户目标与首要约束

用户已经手动把脚与鞋子的位置、姿势调好。需要解决转动脚时鞋子不会同步变形而造成的穿模，并且**绑定瞬间不能改变当前脚与鞋子的可见位置**。

本次优先级：

1. 保留当前脚、鞋子和骨骼姿势。
2. 让鞋子随脚踝、脚背和脚趾的骨骼变形一起变化。
3. 用实际顶点坐标验证没有跳位，然后测试转动。
4. 保留原文件和修复前备份，修复结果另存。

“保持位置”指修改前后、以及测试恢复后的位置一致；正常转动骨骼时，鞋子当然需要运动和变形。

## 2. 实际发现的问题

当前场景存在多套角色骨架，不能凭对象名称相似或材质相同就选择目标。本次活动骨架为 `达妮娅_arm`，身体为 `达妮娅_mesh`。

四个可见鞋部件的初始状态如下：

| 对象 | 顶点数 | 初始父级类型 | 初始父级骨骼 | 修改器 |
| --- | ---: | --- | --- | --- |
| 左鞋子_鞋面 | 692 | BONE | 足首D.L | 无 |
| 右鞋子_鞋面 | 692 | BONE | 足首D.R | 无 |
| 左鞋子_脚部 | 849 | BONE | 足首D.L | 无 |
| 右鞋子_脚部 | 854 | BONE | 足首D.R | 无 |

虽然鞋子还保留了顶点组和形态键，但没有启用骨骼变形修改器。骨骼父级只能让整个对象跟着一根骨骼运动，无法复现身体脚部的混合权重变形。因此，脚趾、脚背与鞋子会产生不同的运动。

身体已有 `mmd_armature` 修改器，目标为 `达妮娅_arm`；本次 `use_deform_preserve_volume=False`。脚部实际权重涉及 `ひざD.*`、`足首D.*`、`足先EX.*` 以及多个脚趾骨骼。

鞋子还带着已经调整好的非单位世界变换。直接给它们加骨骼修改器，可能叠加骨骼父级运动，且会按骨架原来的静置关系重新变形，造成绑定跳位。

## 3. 本次采用的思路

**用身体脚部生成两个隐藏的变形参考网格，再让鞋子通过 Surface Deform（表面变形）跟随它们；绑定基准就是用户当前调好的姿势。**

数据关系：

```text
达妮娅_arm 的原有脚部骨骼
  ├─ 原身体的 Armature 修改器 → 身体脚部变形
  └─ 隐藏参考网格的 Armature 修改器 → 参考脚部变形
       └─ 鞋子的 Surface Deform → 鞋子随参考表面变形
```

鞋子自身不增加 Armature 修改器。其父级从“某根脚踝骨骼”改为“骨架对象”，同时恢复原世界矩阵；脚部局部运动由 Surface Deform 负责，避免重复跟随。

这里区分两个基准：

- 参考网格从身体的**原始网格坐标和骨骼权重**构建，因此它仍可按身体的原有骨架变形。
- Surface Deform 在**当前姿态下的求值结果**上绑定，保存鞋子与当时参考表面的关系。

不要把已经受骨骼变形的求值坐标复制成参考网格之后，再不加补偿地套一次同样的 Armature；那会重复施加姿态变形。

## 4. 实施步骤与关键代码

### 4.1 先读取、记录、备份

修改前记录：

- 当前文件、活动对象、选择、模式、当前帧。
- 四个鞋部件的 `parent`、`parent_type`、`parent_bone`。
- `matrix_world`、`matrix_parent_inverse`、`matrix_basis`。
- 骨骼姿态，以及身体和鞋子的求值后世界空间顶点。

本次用 `save_as_mainfile(..., copy=True)` 保存完整场景副本，不切换当前文件。后续旋转测试另行缓存各骨骼的位置、旋转和缩放通道，用 `try/finally` 恢复。

只检查对象 Location/Rotation/Scale 不够：修改器改变顶点时，对象变换值可以完全不变。

### 4.2 从身体提取左右脚部参考表面

本次限定条件：

- 来源：`达妮娅_mesh`。
- 来源材质索引：`24`，实际材质名为 `Body`。
- 只保留所有顶点均满足局部 `z < 0.34` 的面。
- 左侧要求局部 `x > 0`，右侧要求局部 `x < 0`。
- 本次身体世界矩阵为单位矩阵；这些数值与轴向只适用于本场景。

实际低处还存在 `Nails` 和 `丝袜` 材质。没有将指甲、丝袜重复表面一起作为参考，避免多层表面干扰绑定。

构建参考网格时：

1. 建立原顶点索引到新顶点索引的映射，复制原始坐标和面。
2. 按原顺序创建顶点组；只复制名称能匹配目标骨架骨骼的权重。
3. 仅在参考网格上合并重复点、清理退化边并三角化。
4. 加入指向 `达妮娅_arm` 的 Armature 修改器，保持与身体相同的保体积设置。
5. 使参考对象与身体处于一致的世界空间和骨架父级关系。

本次清理阈值为：合并重复点 `1e-6`，退化清理 `1e-7`。阈值随模型尺度改变，不要照搬到其他模型。

生成的集合与对象：

```text
鞋子变形_辅助
  鞋子变形参考.L
  鞋子变形参考.R
```

绑定完成后使用 `hide_set(True)` 隐藏参考对象，并设置 `hide_render=True`。当前测试确认在此状态下它们仍能驱动鞋子。不要删除参考对象，也不要随意排除其所在集合或关闭参考对象的骨骼修改器。

### 4.3 解除骨骼父级跟随，同时保持世界矩阵

以下是原脚本中的核心逻辑，前提是 `shoe.parent` 已经是目标骨架对象：

```python
world_before = shoe.matrix_world.copy()

shoe.parent_type = 'OBJECT'
shoe.parent_bone = ''
shoe.matrix_parent_inverse = arm.matrix_world.inverted()
shoe.matrix_world = world_before

bpy.context.view_layer.update()
```

迁移到其他场景时，必须确认并设置正确的 `parent`，并同时检查约束、驱动、动画和已有修改器；本次四个鞋部件没有对象约束。

**不要清空脚踝姿态、应用当前姿态为静置姿态、重算自动权重，或移动鞋子来抵消绑定偏移。** 这些都不是本次方案的一部分。

### 4.4 在当前贴合姿势绑定 Surface Deform

```python
mod = shoe.modifiers.new(
    '鞋子随脚变形_保持当前贴合', 'SURFACE_DEFORM'
)
mod.target = foot_reference
mod.falloff = 4

bpy.context.view_layer.update()
with bpy.context.temp_override(object=shoe, active_object=shoe):
    bpy.ops.object.surfacedeform_bind(modifier=mod.name)

assert mod.is_bound
```

原脚本先进入 Object 模式，完成后恢复用户的活动对象、选择和 Pose 模式。身体、鞋子的原始几何、形态键和骨骼静置姿态均未改写。

绑定操作具有切换性质：已经绑定时再次调用会解除绑定。后续维护必须先检查 `is_bound`，不要无条件执行。

### 4.5 验证绑定前后顶点没有跳位

```python
def evaluated_world_vertices(obj):
    bpy.context.view_layer.update()
    depsgraph = bpy.context.evaluated_depsgraph_get()
    evaluated = obj.evaluated_get(depsgraph)
    return np.array([
        evaluated.matrix_world @ v.co
        for v in evaluated.data.vertices
    ])

max_error = np.max(np.linalg.norm(after - before, axis=1))
assert max_error < 1e-6
```

这里按相同顶点索引逐点比较，前提是顶点数量及顺序未改变。本次鞋子的 Surface Deform 不改变拓扑。容差 `1e-6` 为本场景使用的 Blender 单位容差，不是通用毫米标准。

绑定失败或位置超出容差时，移除本次新增修改器，并恢复原父级类型、父级骨骼、父级逆矩阵和对象基矩阵。不要继续在已经跳位的结果上调整鞋子。

## 5. 实际验证结果

### 原姿势的最大世界空间顶点误差

| 对象 | 最大误差（Blender 单位） |
| --- | ---: |
| 达妮娅_mesh | 0 |
| 左鞋子_鞋面 | 1.077117914860537e-7 |
| 右鞋子_鞋面 | 1.4161004457946547e-7 |
| 左鞋子_脚部 | 2.9876736107325996e-7 |
| 右鞋子_脚部 | 1.55215460040381e-7 |

四个修改器均成功绑定。旋转测试结束后恢复原始通道，再次测量得到同样的误差水平；没有可见跳位。

### 旋转测试

两侧同时相对各自原始旋转测试：

- `足首D.L/R`：局部 X ±25°、局部 Y ±15°、局部 Z ±15°。
- `足先EX.L/R`：局部 X ±15°。

测试使用 `original_quaternion @ Quaternion(local_axis, delta)`，不是世界轴的绝对角度，也没有插入关键帧。

利用 BVH 检测身体脚部皮肤面与鞋子面的交叠，并通过脚踝矩阵变化模拟原来的整块骨骼父级跟随，进行对比。下面是四个部件交叠面对数的合计：

| 姿态增量 | 原整块跟随的模拟结果 | 修复后 |
| --- | ---: | ---: |
| 原姿势 | 60 | 60 |
| 脚踝 X -25° | 238 | 60 |
| 脚踝 X +25° | 415 | 60 |
| 脚趾 X -15° | 311 | 60 |
| 脚趾 X +15° | 60 | 60 |

主要转动导致的大量新增交叠消失。原姿势的 60 对分布为：左鞋面 34、右鞋面 19、左脚部 3、右脚部 0。因为要求保持原位置，本次没有移动这些原有交叠。

**已知残留：**脚踝局部 Y +15° 时，右鞋面从 19 对增至 21 对。尝试 Surface Deform 的 falloff 为 4、8、16，结果相同，最终保留 4。后续不要把继续增大 falloff 当成已经有效的修复方向。

交叠面对数是诊断指标，不等于穿透深度、穿透面积或可见穿模程度；此次检测仅覆盖提取范围内的 Body 皮肤表面，未覆盖所有材质、鞋子自交和地面碰撞。不能据此声称所有姿势都无穿模。

本次还检查了当前视角下双脚脚踝 +25° 的截图，以及恢复原姿势后的截图。

## 6. Luna 后续操作时的重点

### 已完成场景不要重新初始化

优先打开修复版，检查四个 Surface Deform 是否绑定、左右参考目标是否正确，然后直接使用原骨骼控制。

`build_shoe_surface_deform.py` 是本次从原始未修复场景出发的实施脚本，**不是幂等维护脚本**。不要在已经修复的场景中直接重跑：它会新增对象和修改器，并覆写原姿势的坐标参考文件。诊断报告中保存了固定的原始参考，不要把失败后的状态覆盖为新基准。

如果后续需要编写维护工具，应先检查对象、集合、修改器和绑定状态，对已有结果做定点更新。不要依赖 Blender 自动生成的 `.001` 后缀来判断目标。

### 处理剩余鞋帮交叠的建议

以下是后续建议，尚未实施：

1. 从右踝局部 Y +15° 的可复现姿态入手，定位新增交叠的具体面与顶点，并检查近景和侧面。
2. 区分原有交叠、仅有计数变化的接触，以及实际新增可见穿透。
3. 如需姿态修形，考虑在特定侧转姿态下生效的局部修正；在当前基准姿态修正量必须为零。
4. 重新验证基准姿态坐标误差，以及受影响的转动范围；测试结束恢复原通道。

不能为了清掉原有交叠数而直接把鞋子整体放大、外推或把脚缩小，那会破坏用户最重要的位置约束。

### 适用范围与未验证事项

- 这是绑定式变形，不是实时碰撞求解，不能保证任意极端姿势下鞋子、皮肤和地面都不相交。
- 参考对象复制了身体的原始几何与骨骼权重，**没有复制身体的形态键驱动链**。当前身体形态键除 Basis 外均为零；后续若使用平足、高跟足、小腿调整等形态键，需要另外让参考表面同步，并重新验证。
- 身体权重或拓扑后续若有变化，已有参考对象不会自动复制这些编辑。
- 本次只对脚踝和脚趾旋转做了上述测试；未全面验证 IK 平移、整套动作、极端屈伸、渲染或导出流程。
- 修复文件已保存，当前会话已验证；未做关闭 Blender 后重新打开的独立验证。
- 材质索引、对象名、坐标裁切范围、单位尺度和原修改器栈都是本场景条件。迁移到其他角色时必须重新检查。

## 7. 文件与证据

项目根目录：`D:/project/blender_learning`。

| 用途 | 文件 |
| --- | --- |
| 修复版，当前交付结果 | [达妮娅_鞋子变形修复.blend](./达妮娅_鞋子变形修复.blend) |
| 原项目，未覆盖 | [达妮娅.blend](./达妮娅.blend) |
| 包含修改前当前姿势的完整副本 | [before_shoe_deform.blend](./validation/shoe_fix_20260909/before_shoe_deform.blend) |
| 实施脚本，阅读参考，勿直接重跑 | [build_shoe_surface_deform.py](./scripts/build_shoe_surface_deform.py) |
| 旋转与交叠对比测试 | [test_shoe_deformation.py](./scripts/test_shoe_deformation.py) |
| 右鞋帮 falloff 对比脚本 | [refine_shoe_cuff.py](./scripts/refine_shoe_cuff.py) |
| 绑定与保位报告 | [binding_report.json](./validation/shoe_fix_20260909/binding_report.json) |
| 旋转测试完整报告 | [rotation_test.json](./validation/shoe_fix_20260909/rotation_test.json) |
| 右鞋帮参数对比结果 | [cuff_refinement.json](./validation/shoe_fix_20260909/cuff_refinement.json) |
| 最终保位核验 | [final_verification.json](./validation/shoe_fix_20260909/final_verification.json) |
| 修复前截图 | [before.png](./validation/shoe_fix_20260909/before.png) |
| 双脚脚踝 +25° 截图 | [ankle_plus25.png](./validation/shoe_fix_20260909/ankle_plus25.png) |
| 恢复原姿势后的截图 | [after_restored.png](./validation/shoe_fix_20260909/after_restored.png) |

同一验证目录中另有身体、四个鞋部件的 `*_before.npy` 世界坐标数组，以及 `pose_reference.json` 骨骼基矩阵记录。原姿势以完整 `.blend` 备份及这些测量数据为准。

## 8. 本项目的 Blender 连接方式

本次通过本地 Blender MCP 插件的 TCP 9876 端口执行 Blender 内的 Python，已有客户端：

```powershell
node scripts/blender_socket_client.mjs get_scene_info
node scripts/blender_socket_client.mjs execute_file scripts/某个经过检查的脚本.py
```

这些命令在项目根目录执行。`execute_file` 会把脚本内容交给正在运行的 Blender；脚本中的 `bpy` 使用的是当前打开场景。不要用普通系统 Python 直接执行需要 `bpy` 的脚本，也不要连接后未经检查就修改当前场景。

本次 API 返回的版本为 `5.2.1 LTS`；`Bone.select` 在该环境不可用，初次检查因此报错。读取选择状态时需要适配当前 API，不能把读取失败当成“没有选择骨骼”。

## 9. 可直接交给 Luna 的任务提示

> 先阅读本指导及修复结果，只读确认当前场景。必须保持用户现有脚和鞋子的世界空间位置及姿势，禁止清姿态或重新自动权重。现有方案是隐藏脚部参考网格的 Armature 驱动，加上鞋子的 Surface Deform，并在原贴合姿势绑定。四个鞋部件已完成绑定，不要重跑初始化脚本。后续如需处理右脚侧转时鞋帮的轻微交叠，请从可复现测试姿态定位问题，采用基准姿态修正量为零的局部方案，并逐顶点核验绑定或修正前后的坐标，最后恢复原姿态。报告必须区分已验证结果、原有交叠和未验证范围。
