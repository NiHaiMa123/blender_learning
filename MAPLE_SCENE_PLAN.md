# 雾中红枫场景 · Blender 建模计划

参考:`参考图片.jpg`(对马岛之魂风格:低机位广角,巨大红枫 + 落叶毯 + 雾中针叶林)

## 目标与技术决策

| 项目 | 决策 |
|------|------|
| 渲染引擎 | Cycles(开发期用 Eevee/Workbench 预览迭代) |
| 输出 | 静帧为主 + 可开关的飘叶动画 collection |
| 枫叶资产 | 全程序化生成(单片掌状裂叶 mesh + GN 实例化) |
| 画幅 | 2560×1080(21:9 宽画幅) |
| 远程看图 | 预览/成品图推送到 GitHub 公开仓库 |
| 执行方式 | `scripts/maple_scene/` 分模块 .py,经 `blender_socket_client.mjs execute_file` 送入 Blender MCP socket (localhost:9876) |

## 画面拆解 → 要实现的东西

1. **程序化枫叶**:掌状 5–7 裂片 + 锯齿缘 + 叶柄;Principled+Translucent 透光材质,Object Info Random 驱动深红→橙→枯黄
2. **枫树主体**:Curve+taper 手工摆 S 形扭曲树干/主枝,末端细枝分叉;深色噪声树皮 + 根部苔藓;GN 在末端枝条散布叶片(外密内疏)
3. **地面落叶毯**:起伏地面 mesh + GN 数千叶片实例(贴地随机旋转,根部加厚);树根石块/小灌木
4. **中远景**:绿色草地条带;右侧石灯笼+木桩(实体简模);左侧日式建筑屋顶剪影;3–4 排低模针叶树纵深;远山剪影;右侧白桦细树
5. **光照氛围**:右后方低角度暖色 Sun(背光);Nishita 天空;世界体积散射做蓝雾;叶片透光调试到"发光"
6. **飘叶**:空中 ~200 片散布(静帧)+ Wind/Turbulence 动画(独立 collection 可开关)

## 执行阶段

- **Phase 0–1**:Cycles/分辨率/AgX;相机 ~28mm 低机位微仰视,占位体块定构图(树占左 2/3,右上留天空亮区)
- **Phase 2**:枫叶 mesh 生成脚本 + 叶材质,单片/小簇渲染测试
- **Phase 3**:树干曲线骨架 → 预览迭代姿态 → 树皮材质 → 树冠布叶
- **Phase 4**:地面 + 落叶毯
- **Phase 5**:中远景元素
- **Phase 6**:光照 + 体积雾整体调校
- **Phase 7**:飘叶 collection + Cycles 终渲(128–256 采样 + denoise)→ `renders/`

## 风险点

- 树干姿态需多轮预览迭代(画面成败关键)
- 叶片透光强度需调试;过强会假,过弱不发光
- Cycles 大实例量注意显存 → 全程实例化,不转真实网格
- 背景雾密度与针叶林剪影层次要平衡

## Git 策略

- `.gitignore` 排除 `*.blend`、`cache/`、`备份/` 等大文件
- 只跟踪:计划文档、`scripts/`、`renders/` 图片
- 每个 Phase 完成即渲染预览 → push → 远程在 GitHub 查看
